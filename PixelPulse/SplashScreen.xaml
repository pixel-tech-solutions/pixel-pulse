<Window x:Class="PixelPulse.SplashScreen"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Pixel Pulse" 
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterScreen"
        Width="500" Height="350"
        ShowInTaskbar="False">
    <Window.Resources>
        <LinearGradientBrush x:Key="BackgroundGradient" StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="#1F2937" Offset="0"/>
            <GradientStop Color="#111827" Offset="1"/>
        </LinearGradientBrush>
        <LinearGradientBrush x:Key="TitleGradient" StartPoint="0,0" EndPoint="1,0">
            <GradientStop Color="#6B46C1" Offset="0"/>
            <GradientStop Color="#3B82F6" Offset="1"/>
        </LinearGradientBrush>
    </Window.Resources>
    
    <Border Background="{StaticResource BackgroundGradient}" CornerRadius="10" BorderBrush="#3B82F6" BorderThickness="2">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Logo/Icon Area -->
            <StackPanel Grid.Row="1" HorizontalAlignment="Center" Margin="0,30,0,20">
                <Ellipse Width="80" Height="80" Stroke="#3B82F6" StrokeThickness="3">
                    <Ellipse.Fill>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="#6B46C1" Offset="0"/>
                            <GradientStop Color="#3B82F6" Offset="1"/>
                        </LinearGradientBrush>
                    </Ellipse.Fill>
                </Ellipse>
                <TextBlock Text="P" 
                          FontSize="48" 
                          FontWeight="Bold" 
                          Foreground="White"
                          HorizontalAlignment="Center"
                          Margin="0,-70,0,0"/>
            </StackPanel>

            <!-- Title -->
            <StackPanel Grid.Row="2" HorizontalAlignment="Center">
                <TextBlock Text="Pixel Pulse" 
                          FontSize="36" 
                          FontWeight="Bold"
                          Foreground="White"
                          HorizontalAlignment="Center">
                    <TextBlock.Effect>
                        <DropShadowEffect Color="#6B46C1" BlurRadius="10" ShadowDepth="0"/>
                    </TextBlock.Effect>
                </TextBlock>
                <TextBlock Text="Inspiration at Your Fingertips" 
                          FontSize="14" 
                          Foreground="#9CA3AF"
                          HorizontalAlignment="Center"
                          Margin="0,5,0,0"/>
            </StackPanel>

            <!-- Loading Indicator -->
            <StackPanel Grid.Row="3" HorizontalAlignment="Center" Margin="0,30,0,30">
                <TextBlock Text="Loading..." 
                          FontSize="12" 
                          Foreground="#6B82F6"
                          HorizontalAlignment="Center"
                          Margin="0,0,0,10"/>
                <ProgressBar IsIndeterminate="True" 
                            Width="200" 
                            Height="4"
                            Background="#374151"
                            Foreground="{StaticResource TitleGradient}"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
